{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
<div class="content">
    <h1>Transaction creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="row">
   <div class="col-12 text-center">
        <input type="submit" value="Contribué" />
        </div>
</div>
    {{ form_end(form) }}


            <a href="{{ path('transaction_index',  {'idMoneyPot' : idMoneyPot}) }}">Retour à la liste</a>
</div>
</div>
{% endblock %}
